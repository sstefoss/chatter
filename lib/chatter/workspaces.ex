defmodule Chatter.Workspaces do
  def change_workspace(%Workspace{} = _workspace, _attrs \\ %{}) do
  end

  def list_workspaces_for_user(_user_id) do
  end

  def get_workspace(_id) do
  end

  def create_workspace(_attrs \\ %{}) do
  end

  def update_workspace(%Workspace{} = _workspace, _attrs) do
  end

  def delete_wokspace(%Workspace{} = _workspace) do
  end
end
