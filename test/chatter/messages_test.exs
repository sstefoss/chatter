defmodule Chatter.MessagesTest do
  use Chatter.DataCase

  describe "list_messages_for_channel/1" do
    test "returns the message for the given channel id" do
    end
  end

  describe "list_direct_messages_for_user/2" do
    test "returns the direct messages a user received" do
    end
  end

  describe "create_message_in_channel/2" do
    test "creates a message that will be postedd in the given channel id" do
    end
  end

  describe "create_direct_message/2" do
    test "creates a message that will be sent to the give user id" do
    end
  end

  describe "update_message/1" do
    test "updates a message with the given attributes" do
    end
  end
end
